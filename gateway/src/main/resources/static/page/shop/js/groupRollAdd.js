var data;
var shopId = '1';
layui.use(['table', 'form'], function () {
    var table = layui.table;
    var $ = layui.jquery;
    var form = layui.form
    form.render();

    form.on('submit(groupRollSubmit)',function (data) {

    })
})